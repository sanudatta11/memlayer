---
title: Persistent memory for AI coding agents
description: Persistent memory for AI coding agents — local, per-project, no cloud.
template: splash
hero:
  title: memlayer
  tagline: Persistent memory for AI coding agents — local, per-project, no cloud.
  actions:
    - text: Get started
      link: /docs/getting-started/
      icon: right-arrow
    - text: GitHub
      link: https://github.com/sanudatta11/memlayer
      icon: external
      variant: minimal
---

memlayer stores decisions, patterns, fixes, and notes in SQLite and surfaces
the right ones when you (or your agent) start the next session. A thin CLI
talks to a per-user daemon over gRPC; agents can also use MCP tools or
shell hooks.

```bash
git clone https://github.com/sanudatta11/memlayer && cd memlayer
make prereqs && make install
export PATH="$HOME/.local/bin:$PATH"
memlayer --version
```

Works with Claude Code, Cursor, Windsurf, Antigravity, OpenCode, Kimi Code,
ZCode, VS Code / Copilot, Codex, Gemini CLI, and any agent that can shell
out or speak MCP.
